$schema: https://intentional.foo/schemas/config.yml
contract: contract-2
release-units:
  garden-notes:
    path: .
    tags:
      primary:
        role: primary
        template: "{version}"
    projections:
      - adapter: npm
        file: package.json
        mode: committed
    packages:
      garden-notes:
        path: .
