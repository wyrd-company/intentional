---
intentional: patch
---

Refuse a managed job substitution naming a placeholder its template does not carry, so an entry orphaned by a template change is reported instead of silently doing nothing.
