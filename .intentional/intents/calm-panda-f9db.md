---
intentional: minor
---

Project the verified annotated global tag object from tag verification, so a consumer can bind evidence to the tag object the publish run proved rather than to a name alone.
